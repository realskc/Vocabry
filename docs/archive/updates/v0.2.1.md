# v0.2.1：Word Query 词组查询

v0.2.1 将 Word Query 的查询范围从单个英语单词扩展到英语词组。输入允许空格、连字符、撇号、英文句点和 Unicode 省略号，并要求至少包含一个英文字母，因此可以查询 `take off`、`the more ... the more` 和 `the more … the more` 等表达。

GUI 引导与 DeepSeek prompt 同步使用“单词或词组”的产品语义。Anki add-on 未在本次更新中改变，继续使用其独立的 0.2.0 组件版本。
